{
  "name": "@vibesphere/rag",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@vibesphere/database": "workspace:*",
    "@vibesphere/llm": "workspace:*"
  },
  "devDependencies": {
    "@vibesphere/config": "workspace:*",
    "typescript": "^5.6.2"
  }
}
